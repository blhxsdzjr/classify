# Generated/used by src.prepare_local_dataset for the files currently in this
# project root:
#   - 道路example.zip
#   - test(1).zip
#   - 结果统计.xlsx
#
# Important: the primary training strategy is one-class lane detection:
# train YOLO to find lane_line only, then classify white/yellow in
# src.predict_yolo_lane with HSV post-processing.
#
# The provided zip files contain images only. YOLO training still needs
# matching labels under labels/train and labels/val.
path: datasets/local_colm
train: images/train
val: images/val
test: images/test

names:
  0: lane_line
